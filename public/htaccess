<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect to index.html if the request is not for an actual file or directory
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [QSA,L]
</IfModule>